# macOS
.DS_Store
.AppleDouble
.LSOverride
*.swp

# Local settings & config (but track the org's shared skills/commands)
.claude/*
!.claude/skills/
!.claude/commands/
.vscode/mcp.json
node_modules/
.env

# Self-hosted SDK copy (generated by `npm run vendor-sdk`)
vendor/

# Unused / archived material — kept locally, out of the repo
misc/
